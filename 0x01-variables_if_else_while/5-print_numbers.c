#include <stdio.h>

/**
 * main - entry point
 *
 * Prints alphabet in lower case
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

	for (ch = 1; ch <= 10; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
