#include <stdio.h>

/**
 * main - entry point
 *
 * Prints alphabet in lower case
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		if (ch = 'q')
		{
			return (0);
		}
		else if (ch = 'e')
		{
			return (0);
		}
		else
		{
			putchar(ch);
		}
	}
	for
	putchar('\n');
	return (0);
}
