#include "holberton.h"

/**
 * swap_int - Swaps the integer values
 * @a: first integer
 * @b: second integer
 *
 * Swaps one integer with another
 * Return 0 Always (Success)
 */

int _strlen(char *s)
{
	int str[];
	int n;

	while (str[n] != '\0')
	{
		n++;
	}

	return (n);
}
