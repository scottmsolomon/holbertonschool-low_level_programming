#include "holberton.h"

/**
 * main - prints the alphabet lower case
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char n;

	for (n = 'a'; n <= 'z'; n++)
	{
		_putchar(n);
	}

		_putchar('\n');

	return (0);
}
